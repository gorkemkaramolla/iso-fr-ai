# import oracledb
# from config import DB_USER, DB_PASSWORD, DB_PORT, DB_SERVICE_NAME, DB_HOST

# connection = oracledb.connect(
#     user=DB_USER,
#     password=DB_PASSWORD,
#     host=DB_HOST,
#     port=DB_PORT,
#     service_name=DB_SERVICE_NAME
# )
# cursor = connection.cursor()
# LOB = oracledb.LOB;
# ERROR = oracledb.Error;